services:
- type: web
  name: anemia-ai-server
  env: python
  
  envVars:
    - key: PYTHON_VERSION
      value: 3.11.9 

  buildCommand: pip install -r requirements.txt
  startCommand: gunicorn server:app
